[workspace]
resolver = "2"
members = [
    "contracts/syndicate",
    "contracts/campaign",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
strip = true
lto = true
codegen-units = 1
panic = "abort"

[profile.release-with-logs]
inherits = "release"
debug = true
