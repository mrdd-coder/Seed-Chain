[package]
name = "seedchain-syndicate"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "21.0.1" }

[dev-dependencies]
soroban-sdk = { version = "21.0.1", features = ["testutils"] }
